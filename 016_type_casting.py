# we can convert one type of variable into another (casting) using the int() method and also the float() method
# there's also a str() method
anything = float(input("Enter a number: "))
something = anything ** 2.0
print(anything, "to the power of 2 is", something)
